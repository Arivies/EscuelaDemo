{% extends '::front.html.twig' %}

{% block content %}
<div class="row">
    <div class="large-5 large-centered columns">
    	{{ form_start(formulario) }} 
			{{ form_label(formulario.nocontrol, "No. de control") }}
			{{ form_widget(formulario.nocontrol) }}
			{{ form_label(formulario.nombre) }}
			{{ form_widget(formulario.nombre) }}
			{{ form_label(formulario.apellidoPaterno) }}
			{{ form_widget(formulario.apellidoPaterno) }}
			{{ form_label(formulario.apellidoMaterno) }}
			{{ form_widget(formulario.apellidoMaterno) }}
			{{ form_label(formulario.carrera) }}
			{{ form_widget(formulario.carrera) }}
			<input type="submit" value="GUARDAR" >
		{{ form_end(formulario) }}
	</div>
</div>
{% endblock %}